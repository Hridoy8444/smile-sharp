import React from 'react';
import { Card } from 'react-bootstrap';

const Wedding = ({wedding}) => {
    const {img} = wedding;
    return (
        <div>
            
        </div>
    );
};

export default Wedding;